.h3
  Posts index page

- @posts.each do |post|
  .post
    .h4.post_title
      = link_to post.title, post
    .p.post_published
      Published at 
      = time_ago_in_words(post.created_at)
      ago, by 
      = post.user.email

/ = link_to "New Post", new_post_path